// Import global npm modules
import { MotionProps } from 'framer-motion'

// Create framer api
const framer = <MotionProps>{
  whileTap: {
    opacity: 0.8,
    scale: 0.98
  }
}

// Export default framer api
export default framer